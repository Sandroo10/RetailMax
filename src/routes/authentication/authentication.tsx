import SignUpForm from '../../components/sign-up-form/sign-up-form';

const Authentication = () => {
    return (
        <div className="flex justify-around max-w-[1000px] my-12 mx-auto">
            <SignUpForm />
        </div>
    );
};

export default Authentication;
